# Setup — to be filled in step 2

This file becomes the "how to run it on your Mac" guide once the backend
skeleton lands. Right now the repo is just scaffolding (step 1).

Next time you talk to me, ask to do **step 2: backend skeleton**. That gives us:
- A FastAPI app you can actually start with `uvicorn app.main:app --reload`
- A `/health` endpoint to confirm the server is alive
- Config loading from `.env`
- Structured logging
- A first `pytest` test that passes

After that I'll come back and fill in this file with the exact commands you
need to run on macOS to install Python 3.11, create a virtualenv, install deps,
and start the server.
